# Revisión 002 — Fases 4 a 7 (Opus, solo lectura)

Hallazgos verificados con sondas sobre HEAD `c778a5d`. Orden: más severo primero.

| # | Severidad | Hallazgo | Fix mínimo | Estado |
|---|---|---|---|---|
| 1 | **alta** | Tras la transición de gobierno (`simulation.py:563`) los actores por reglas conservan la tabla de partidos capturada al construirse (`rule_based.py:502`, `scheduler.py:211`): `_in_government()` devuelve el partido viejo todo el segundo mandato, invirtiendo el signo de `electoral_pressure`, `_impact_reelection` y la distancia editorial de los medios. `FakeBackend("rules")` cachea lo mismo desde disco. | Refrescar `parties_by_id` de cada `decision_actor` en `_run_election`; que `FakeBackend` reutilice `engine.decision_actors` o se reconstruya en la transición. | pendiente |
| 2 | **alta** | `v_evt · recent_events_c` (`elections.py:292`) no depende del partido, así que se cancela en el softmax: las memorias de cohorte (`shock_hit`, `forced_devaluation`) no afectan el voto. Sonda: con y sin memorias, shares byte a byte iguales. | Aplicar el término solo al oficialismo (`if p.in_government`), como los otros dos de castigo. | pendiente |
| 3 | media-alta | `months_to_election` sigue siendo el placeholder de Fase 3 (`simulation.py:658`: `months − month + 1`). En corridas de 96 meses nadie siente presión electoral antes del mes 48 (`elec = 0`), pero `CAMPAIGN` sí está autorizada. | `T = term_length; ((month−1)//T + 1)·T − month` si `elections_enabled`, si no el placeholder (golden hashes intactos). | pendiente |
| 4 | media | `authority_violation` (`evals/metrics.py:180`) cuenta toda denegación (cooldown, presupuesto, params, gobernanza), no solo las de rol; contradice ADR 007 §2 y la misma métrica en `cli.py:861`. | Filtrar por el marcador `"no tiene permitido"` desde una constante compartida. | pendiente |
| 5 | baja-media | Una concesión ejecutada de inmediato pasa a `honored` en el mismo turno (`negotiation.py:401`) y el Congreso solo bonifica `vigente` (`congress.py:139`): nunca compra el voto por el que se negoció (22 de 56 acuerdos en 96 meses). | Aceptar `honored` con `granted_month == bill.month`, o diferir la transición al fin del turno. | pendiente |
| 6 | baja | `llm_based.py:87` lista acciones disponibles solo desde la matriz de rol, no desde `write|execute` de gobernanza. `frame_by_outlet` (`simulation.py:931`) solo drifta audiencia con el último frame si un medio publica dos veces. | Pasar `gov.write \| gov.execute`; acumular frames. | pendiente |
| 7 | baja | Código muerto: `last_counter_concession`, `MemoryStore.score_term`, `judge.perception_numbers_json`, `retrieve(situation_kind=…)` nunca pasado, texto "sin memorias todavía" en `prompts.py:230`, parámetro `judge` sin uso en dos métricas. | Limpiar; cablear `situation_kind` desde el tipo de propuesta. | pendiente |
| 8 | tests | `test_two_terms…` solo cuenta registros; `test_discipline_one…` no puede fallar; `test_uniform_ideology…` es simétrico por construcción; `test_fake_unauthorized…` solo asierta `> 0`. | Asertar `parties_by_id == sim.country.parties` tras transición; que `authority_violation` iguale la razón de denegaciones por rol; test de `v_evt` con memorias negativas bajando al oficialismo. | pendiente |

Nota metodológica: `ideological_consistency = 1.0` contra `rules` es circular (los casos se generaron desde las reglas); vale como test de tubería, no como evidencia de que las reglas sean correctas. Los 20 casos `author: human` siguen pendientes del usuario.

Verificado y correcto: orden del turno (ADR 005 §5), gobernanza antes de la matriz de rol y ninguna acción aplicada sin `authorize`, `filter_perception` antes de `decide`, `--governance-override central_bank.autonomy=4` produce `SET_RATE`, matemática del voto en Congreso, ciclo de vida de acuerdos (sin fugas ni doble cobro), cohortes con `pop_share` y medios sin tocar variables reales, `perception_gap` puede ser negativo, memoria (score, k, presupuesto, ≥ 0.8 nunca resumida), D'Hondt, balotaje, luna de miel, re-etiquetado de memorias, juez ≠ actor.
